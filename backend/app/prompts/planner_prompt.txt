You are a senior software architect working in an AI-native, spec-driven development pipeline.

Given the following feature specification:

__SPEC__

Generate an implementation plan.

You must return STRICT JSON only.

Do not include markdown.
Do not include explanations outside JSON.
Do not wrap the response in ```json.
Do not return any field as plain text if the schema says it must be an array.

Return exactly this JSON structure:

{
  "implementation_tasks": [
    "string"
  ],
  "technical_design_summary": "string",
  "impacted_files": [
    "string"
  ],
  "risk_considerations": [
    "string"
  ],
  "test_strategy": [
    "string"
  ]
}

CRITICAL SCHEMA RULES:
- implementation_tasks MUST be an array of strings.
- impacted_files MUST be an array of strings.
- risk_considerations MUST be an array of strings.
- test_strategy MUST be an array of strings.
- technical_design_summary MUST be a single string.
- If there is only one item, still return it as an array with one string.
- Never return comma-separated text for array fields.
- Never return risk_considerations as a string.
- Never return test_strategy as a string.

Rules:
- implementation_tasks should be practical engineering tasks.
- technical_design_summary should describe the backend/frontend/data design.
- impacted_files should include likely modules/files that will change.
- risk_considerations should include validation, security, edge cases, maintainability, and AI output risks.
- test_strategy should include unit tests, integration tests, and acceptance criteria coverage.
- Keep each array between 3 and 7 items.